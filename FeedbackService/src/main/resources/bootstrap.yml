spring:
  application:
    name: feedback-service
  config:
    import: optional:configserver:http://34.218.120.119:8000

  kafka:
    producer:
      properties:
        schema:
          registry:
            url: "http://127.0.0.1:8081"
    admin:
      auto-create: false
    bootstrap-servers: localhost:9092