spring:
  application:
    name: gateway-service

  config:
    import: optional:configserver:http://44.244.227.40:8000
logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    reactor.netty.http.client: DEBUG
