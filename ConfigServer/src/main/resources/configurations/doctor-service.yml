spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/doctors
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect

#logging:
#  level:
#    root: debug

server:
  port: 8020